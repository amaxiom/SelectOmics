"""
setup.py

Setuptools configuration for SelectOmics.

For editable development installs:

    pip install -e .

For a standard install:

    pip install .
"""

from setuptools import setup

# All metadata is declared in pyproject.toml.
# This file exists only for legacy editable-install compatibility (pip install -e .).
setup()
